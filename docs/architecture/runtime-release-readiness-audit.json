{
  "schema_version": 1,
  "campaign": "runtime-release-readiness-p0-p1-finite-closure",
  "phase": "RRP-4.1",
  "phase_title": "Durable Schema Migration and Interrupted Recovery",
  "repository": "globalpocket/brownie",
  "audited_main_commit": "8439b1ef4aebe8cf3f0acda09f86aca31cdfc31d",
  "runtime_release_ready": false,
  "release_ready_blocked_by": [
    "runtime-release-guard-ci",
    "protocol-event-canonization",
    "platform-deadline-durability-hardening"
  ],
  "owner_decisions": [
    {
      "id": "oss_license",
      "status": "required",
      "reason": "Cargo workspace remains UNLICENSED and publish=false, so OSS release basis cannot be declared by Runtime implementation alone."
    }
  ],
  "classifications": [
    {
      "id": "runtime-release-debt-reaudit",
      "title": "Runtime Release debt reaudit",
      "priority": "P0",
      "status": "implemented_sufficient",
      "responsibility_domain": "runtime",
      "debt_classification": "closed",
      "evidence": [
        "README, Product Charter, Product Completion Gate, Runtime Boundary DoD, Brownie Scope, Runtime Protocol, CLI External Loop, Task Runtime, Tool Execution, Permission Gate, ModePack specs, current phase-value manifest, M64.15 manifest, control-plane pointer, Rust crates, Node guards, VSIX check path, and CI workflow were reaudited.",
        "This audit is now machine-checked by scripts/guard-runtime-release-readiness.mjs and human-readable in docs/architecture/runtime-release-readiness-audit.md."
      ],
      "next_action": "Use this artifact as the bounded source for the remaining P0/P1 closure phases."
    },
    {
      "id": "runtime-boundary-protocol-contracts",
      "title": "Boundary Protocol gaps",
      "priority": "P0",
      "status": "implemented_sufficient",
      "responsibility_domain": "runtime",
      "debt_classification": "closed",
      "evidence": [
        "Runtime boundary docs define Run Request, Runtime Event, Control Command, and Run Result/Attestation.",
        "RRP-1 adds docs/architecture/runtime-boundary-canonical-contract.json as the canonical public Runtime boundary contract and compatibility matrix.",
        "scripts/guard-runtime-release-readiness.mjs validates required Runtime boundary surfaces, required public method subset, CLI/VSIX/Runtime/spec anchors, compatibility-matrix entries, and non-authority language through the existing VSIX-invoked release-readiness guard path."
      ],
      "next_action": "Use the canonical Runtime boundary contract as the guarded source for public boundary compatibility while closing the remaining Runtime Release Readiness blockers."
    },
    {
      "id": "explicit-cancel-command",
      "title": "Cancel semantics",
      "priority": "P0",
      "status": "implemented_sufficient",
      "responsibility_domain": "runtime",
      "debt_classification": "closed",
      "evidence": [
        "RRP-2 adds a Runtime-owned task.cancel JSON-RPC boundary with typed protocol params and result.",
        "task.cancel requires caller authorization, concrete task/run identity, expected current status, expected task updated_at, bounded cancel_id, and a Runtime-computed cancel request fingerprint before terminal evidence.",
        "Only Created, Queued, and Running tasks are cancellable; missing, stale, mismatched, completed, failed, malformed, ambiguous, or unrelated requests fail closed before TaskCancelled evidence.",
        "Exact replay is idempotent only when the current durable TaskCancelled payload matches the same cancel request fingerprint; otherwise cancelled tasks fail closed without duplicate terminal evidence.",
        "VSIX protocol validators and runtime client projections cover task.cancel params/results while remaining thin transport validation over Runtime authority."
      ],
      "next_action": "Use task.cancel as the explicit Runtime-owned cancellation boundary while closing the remaining Runtime Release Readiness blockers."
    },
    {
      "id": "real-process-loss-recovery-e2e",
      "title": "Real process loss Recovery E2E",
      "priority": "P0",
      "status": "implemented_sufficient",
      "responsibility_domain": "runtime",
      "debt_classification": "closed",
      "evidence": [
        "RRP-3 replaces create_new MCP approval claim lock authority with an OS-owned advisory file lock over a non-authoritative residual lock file.",
        "RRP-3.1 retains the approval claim lock through executing evidence, actual tools/call, result normalization, and terminal approval append.",
        "terminalize_mcp_tool_approval rereads latest matching approval state before appending terminal evidence and refuses missing, non-executing, or changed approval_state_fingerprint transitions, preventing outcome_unknown-to-consumed terminal chains.",
        "recover_unfinished_mcp_tool_approvals uses a nonblocking attempt on the same execution-scoped lock, skips WouldBlock live owners without ledger mutation, rereads latest matching state under the lock, and appends outcome_unknown at most once only for latest executing evidence.",
        "test:rrp3-process-loss launches actual brownie-runtime child processes with an isolated trusted active Mode Pack snapshot and deterministic fake MCP stdio server, verifies stale lock progress, executing-before-spawn recovery, concurrent recovery without duplicate outcome_unknown, recovery during live tools/call as a no-op, mid-tools/call process loss recovery, independent process racing with at most one tools/call, and terminal consumed non-rerun after restart.",
        "The existing CI-invoked VSIX check path now invokes pnpm --workspace-root test:rrp3-process-loss."
      ],
      "next_action": "Use RRP-3/RRP-3.1 process-loss and race-safety evidence as the local Runtime recovery baseline while closing the remaining Runtime Release Readiness blockers."
    },
    {
      "id": "durable-schema-version-and-migration",
      "title": "Durable schema version and migration",
      "priority": "P0",
      "status": "implemented_sufficient",
      "responsibility_domain": "runtime",
      "debt_classification": "closed",
      "evidence": [
        "RRP-4 added .brownie/store-schema.json as the release-level durable store schema manifest for local Runtime state, with schema id, manifest format version, store schema version, minimum runtime schema version, current state, and bounded migration source only.",
        "RRP-4.1 advances the local durable store schema from v1 to v2 and adds an explicit Runtime/store-owned migration registry with a v1 to v2 migration path.",
        "RRP-4.1 persists a bounded migration_in_progress marker before migration work, writes the v2 store-layout.json marker through the synced atomic helper, and replaces the schema manifest with current v2 only after the marker validates.",
        "First-touch recovery resumes interrupted v1 to v2 migration idempotently after either the in-progress manifest write or the store-layout.json marker write, while conflicting partial migration layout state fails closed before task mutation.",
        "Manifest parsing is strict and validation fails closed for malformed JSON, unknown fields, wrong schema id, unsupported migration state, zero versions, future store schema versions, newer minimum runtime versions, missing current v2 layout markers, or conflicting partial migration state.",
        "BrownieStore::from_env_or_cwd gates store-backed Runtime methods before durable JSON-RPC work proceeds, and task state, run ledger, headless continuation, run session, journey, objective admission, completion finalization, active ModePack, and ModePack candidate durable paths check schema before mutation through their read/write entrypoints. Store-free status remains outside the durable gate.",
        "cargo test -p brownie-store durable_schema -- --test-threads=1 covers v2 manifest creation, missing-manifest adoption, v1 manifest migration, interrupted migration resume before and after layout marker write, partial migration conflict rejection, malformed rejection, future-version rejection before mutation, and minimum-runtime-version rejection.",
        "guard:durable-schema-migration checks that the v1 to v2 registry, migration_in_progress marker, store-layout.json marker, interruption tests, and reopened release blocker accounting remain present."
      ],
      "next_action": "Use the RRP-4.1 durable schema migration and interrupted recovery baseline while closing the reopened protocol semantic contract and platform/deadline/failure-injection blockers before the CI release gate."
    },
    {
      "id": "runtime-release-guard-ci",
      "title": "CI Release Gate",
      "priority": "P0",
      "status": "partial",
      "responsibility_domain": "runtime",
      "debt_classification": "required_before_release",
      "evidence": [
        "RRP-3.1 adds pnpm --workspace-root test:rrp3-process-loss to the existing VSIX check script that CI invokes, making the real process-loss E2E CI-required without changing .github/workflows/ci.yml.",
        "RRP-4.1 adds pnpm --workspace-root guard:durable-schema-migration to the existing VSIX check path and adds focused brownie-store interrupted migration tests, but direct .github workflow wiring remains blocked by missing OAuth workflow scope.",
        "RRP-5 adds guard:protocol-event-canonization to the existing CI-invoked VSIX check path.",
        "RRP-6 adds guard:runtime-module-decomposition to the existing CI-invoked VSIX check path and makes the Runtime release-readiness guard require that VSIX command.",
        "RRP-7 adds guard:platform-deadline-durability to the existing CI-invoked VSIX check path and makes the Runtime release-readiness guard require that VSIX command.",
        "RRD-1 already wires the Runtime release readiness guard into the VSIX check path that CI invokes.",
        "Full CI Release Gate hardening remains a later P0 closure phase for frozen install, fmt, audit/SBOM/secret/dependency, and complete release gate policy coverage."
      ],
      "next_action": "Close direct workflow wiring and remaining full CI release-gate hardening in the dedicated CI phase when workflow-scope credentials are available."
    },
    {
      "id": "mcp-runtime-safety-policy",
      "title": "MCP Runtime Safety Policy finite closure",
      "priority": "P0",
      "status": "implemented_sufficient",
      "responsibility_domain": "runtime",
      "debt_classification": "closed",
      "evidence": [
        "The user supplied a non-conflicting follow-on MCP Runtime Safety Policy campaign after RRD-1.",
        "MCP-S1 closes result error semantics, MCP-S1.1 corrects CallToolResult resultType/isError protocol-conformance edge cases, MCP-S2 closes tool-level Brownie safety policy, MCP-S3 closes annotation provenance/drift checks, MCP-S4 closes tool-level approval binding, MCP-S4.1 closes approval consumption/retry safety, MCP-S5 closes runtime input/output schema validation, MCP-S6 closes the secret reference contract, and MCP-S7 closes executable identity.",
        "MCP stdio execution is now result-safe, policy-bound, annotation/catalog drift checked, approval-state-bound, schema-validated, secret-reference scoped, and executable-identity pinned and rechecked before launch.",
        "MCP approval-required execution uses public mcp.tool.approve, Runtime-owned latest-state approval claims, consumed/outcome_unknown terminalization, and duplicate retry/reuse denial from durable attempt evidence.",
        "MCP server descriptions, schemas, annotations, results, secret references, executable identity, approval state, and model output remain non-authoritative and narrowing-only."
      ],
      "next_action": "Continue the remaining Runtime Release Readiness P0/P1 blockers before claiming Runtime Release Ready."
    },
    {
      "id": "oss-release-technical-basis",
      "title": "OSS Release technical basis",
      "priority": "P1",
      "status": "owner_decision_waiting",
      "responsibility_domain": "owner",
      "debt_classification": "owner_decision",
      "owner_decision_required": "oss_license",
      "evidence": [
        "Workspace Cargo.toml declares license = UNLICENSED and publish = false.",
        "Runtime can prepare technical release evidence, but license/publish posture requires owner decision before OSS Release Ready can be claimed."
      ],
      "next_action": "Record owner license/publish decision before declaring OSS release basis complete."
    },
    {
      "id": "protocol-event-canonization",
      "title": "Protocol/Event canonization",
      "priority": "P1",
      "status": "partial",
      "responsibility_domain": "runtime",
      "debt_classification": "required_before_release",
      "evidence": [
        "RRP-5 adds docs/architecture/runtime-protocol-event-canonical-map.json as the canonical Runtime-owned protocol/event ownership map for JSON-RPC method groups, generated compatibility families, event domains, durable ledger variants, CLI transport subset, VSIX validators/client calls, and docs anchors.",
        "scripts/guard-protocol-event-canonization.mjs extracts Runtime METHOD_* constants and dispatch arms, EventKind variants, and LedgerEventKind variants, then fails when a method/event is unmapped or when declared Runtime, protocol, CLI, VSIX, or docs anchors drift.",
        "The latest release-readiness review reopens this blocker because RRP-5 canonized ownership and drift detection but did not add a machine-readable Rust-generated protocol schema, Rust serialize to JSON to TypeScript semantic golden contract, required/optional/null/enum/error-payload semantics, backward compatibility checks, unknown field or variant policy checks, or durable event migration coupling."
      ],
      "next_action": "Close RRP-5.1 with semantic protocol contract evidence after RRP-4.1 and RRP-7.1."
    },
    {
      "id": "runtime-module-decomposition-reevaluation",
      "title": "Runtime module decomposition reevaluation",
      "priority": "P1",
      "status": "implemented_sufficient",
      "responsibility_domain": "runtime",
      "debt_classification": "closed",
      "evidence": [
        "RRP-6 adds docs/architecture/runtime-module-decomposition-assessment.json as the finite closure assessment for Runtime module decomposition, with exact source metrics for line counts, production/test split, public/private items, state mutation, durable write, permission, recovery, RPC dispatch, approval, MCP, Product DoD, and diagnostics hotspots.",
        "RRP-6 physically moves the task.list handler and task-list transport-bound helpers from crates/brownie-runtime/src/lib.rs into crates/brownie-runtime/src/task_progress.rs, colocating task-list transport bounds with task progress overview and parent-join projection authority without changing JSON-RPC behavior.",
        "scripts/guard-runtime-module-decomposition.mjs validates the assessment against live source metrics, lib.rs production/test ceilings, required boundary tokens, task_progress ownership, and non-authority rules; its tests fail closed for task.list drifting back into lib.rs, missing task_progress ownership, and metric tampering.",
        "extensions/brownie-vsix/package.json now invokes pnpm --workspace-root guard:runtime-module-decomposition through the existing CI-invoked VSIX check path, and scripts/guard-runtime-release-readiness.mjs requires that invocation."
      ],
      "next_action": "Use the guarded RRP-6 module decomposition assessment as the release baseline while closing the remaining Runtime-owned blockers."
    },
    {
      "id": "platform-deadline-durability-hardening",
      "title": "Platform, deadline, and durability hardening",
      "priority": "P1",
      "status": "partial",
      "responsibility_domain": "runtime",
      "debt_classification": "required_before_release",
      "evidence": [
        "RRP-7 adds docs/architecture/runtime-platform-deadline-durability-hardening.json as the finite closure assessment for local Runtime platform/deadline/durability scope.",
        "TaskStore::write_task_state now uses the shared write_file_atomically helper, so task state writes use a sibling temporary file, create_new, file write, file sync, atomic rename, and parent-directory sync like other durable store state.",
        "write_file_atomically now treats Unix parent-directory sync as a checked durability step rather than best-effort ignored evidence, while non-Unix remains an explicit local Runtime boundary no-op.",
        "The latest release-readiness review reopens this blocker because RRP-7 covered only a narrow local subset and still lacks Runtime-wide deadline canonicalization, resume deadline reconstruction, timeout versus terminal append race coverage, cancel versus tool response race coverage, concurrent writer failure injection, disk-full/permission/rename/truncated-state cases, path canonicalization checks, Windows process tree cleanup, Windows atomic replace/directory durability, and macOS/Windows Runtime verification."
      ],
      "next_action": "Close RRP-7.1 with deadline, cross-platform, and failure-injection evidence before RRP-5.1 and the CI release gate."
    },
    {
      "id": "hosted-scheduler-daemon-worker-fleet",
      "title": "Hosted scheduler, daemon, job queue, and worker fleet",
      "priority": "P2",
      "status": "runtime_outside",
      "responsibility_domain": "external_control_plane",
      "debt_classification": "post_v0",
      "evidence": [
        "README, Brownie Scope, Product Charter, and Runtime Boundary DoD classify schedulers, daemons, worker fleets, leases, hosted isolation, metrics, alerts, SLAs, and billing outside Brownie Runtime release."
      ],
      "next_action": "Track outside Runtime Release Readiness."
    },
    {
      "id": "forge-notification-adapters",
      "title": "Forge and notification adapters",
      "priority": "P2",
      "status": "runtime_outside",
      "responsibility_domain": "external_adapter",
      "debt_classification": "post_v0",
      "evidence": [
        "GitHub/GitLab Apps, PR create/update/merge workflows, Slack/Teams/email, SIEM, OTel, and customer-system adapters are external adapter readiness, not Runtime release blockers."
      ],
      "next_action": "Track as adapter product work only after Runtime release closure."
    },
    {
      "id": "enterprise-commercial-readiness",
      "title": "Enterprise, Assurance, and commercial solution readiness",
      "priority": "P2",
      "status": "runtime_outside",
      "responsibility_domain": "commercial_solution",
      "debt_classification": "post_v0",
      "evidence": [
        "Tenant management, SSO/RBAC server, customer admin UI, billing, certified stack, continuity assurance, and broad BDK/Enterprise/Assurance surfaces are outside this Runtime-owned campaign."
      ],
      "next_action": "Do not use these items to block Brownie Runtime release."
    }
  ]
}
