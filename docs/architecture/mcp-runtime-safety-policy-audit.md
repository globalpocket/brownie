# Brownie MCP Runtime Safety Policy Audit

This audit registers the user-requested MCP Runtime Safety Policy finite closure
campaign as a Runtime-owned follow-on to the Runtime Release Readiness campaign.
It preserves the existing MCP-first architecture and does not introduce a
Brownie-specific tool adapter path.

Audited main: `fba0c2c9dbe872f267e77970a620582c4c989f7c`

| ID | Priority | Area | Status | Classification | Evidence summary | Next action |
| --- | --- | --- | --- | --- | --- | --- |
| `mcp-result-error-semantics` | P0 | MCP `isError` and execution status semantics | implemented sufficient | closed | MCP `tools/call` now separates `ProtocolSucceeded`, `ToolSucceeded`, `ToolReturnedError`, `ProtocolFailed`, `TimedOut`, `InputRequiredUnsupported`, and malformed-result `Failed` outcomes while preserving the external `ToolExecuteStatus` enum. `resultType="complete"` and absent backward-compatible `resultType` are accepted, `input_required` fails closed in v0, omitted `isError` defaults to success, and `isError=true` emits `ToolExecutionFailed` with only bounded untrusted context. | Use as the baseline for later MCP safety phases. |
| `mcp-tool-safety-policy` | P0 | Tool-level Brownie safety policy | implemented sufficient | closed | Mode Pack MCP policy now encodes per-tool `side_effect`, `approval`, `idempotency`, and `retry`. Runtime allows approval-free `tools/call` only for structured `read_only`, `approval=not_required`, `idempotency=safe`, non-prohibited retry policy. Legacy string allow-lists compile as unclassified prohibited policy, and mutation/destructive/unknown tools fail closed unless the structured policy qualifies for the Runtime approval-binding path. | Use as the baseline for MCP annotation provenance and approval binding. |
| `mcp-tool-annotation-provenance` | P0 | MCP tool annotations | implemented sufficient | closed | `tools/list` parses bounded MCP annotations for `readOnlyHint`, `destructiveHint`, `idempotentHint`, and `openWorldHint`; catalog entries carry bounded annotations and an annotation fingerprint; catalog fingerprints and task-pinned catalog matching detect annotation drift; invalid annotation field types fail closed; contradictory or open-world annotations narrow approval-free execution before `tools/call`. | Use as the narrowing-only provenance baseline for MCP-S4 approval binding. |
| `mcp-tool-approval-binding` | P0 | Tool-level approval | implemented sufficient | closed | Approval-required MCP `tools/call` execution is bound to a Runtime-materialized approval fingerprint scoped to task/run identity, normalized tool id, server id, tool name, request fingerprint, task-pinned catalog/schema/annotation provenance, server config identity, and structured Brownie MCP safety policy. Missing, stale, consumed, malformed, mismatched, or over-broad approval evidence denies before `tools/call`; exact approved invocation replay reuses completed evidence without a duplicate call. | Use as the baseline for MCP-S5 runtime schema validation. |
| `mcp-runtime-schema-validation` | P0 | Runtime input/output schema validation | implemented sufficient | closed | MCP catalog admission validates `inputSchema` and optional `outputSchema` against a bounded Runtime-owned JSON Schema subset before task-pinning; known unsupported validation keywords fail closed; Runtime validates `tools/call` input before server execution and successful structured output before `ToolExecutionCompleted` or completed-success replay evidence. Schema validation evidence is bounded to fingerprints/status/target metadata without ledgering raw schemas, raw inputs, or raw MCP responses. | Use as the baseline for MCP-S6 secret reference contract. |
| `mcp-secret-reference-contract` | P1 | Secret reference contract | implemented sufficient | closed | Trusted Mode Pack MCP server config now accepts only bounded structured secret-reference environment bindings and rejects raw environment values, malformed bindings, duplicate references, unsupported shapes, and unresolved references fail closed before stdio spawn. Runtime resolves references through a `McpSecretResolver` boundary at each request-scoped `tools/list` or `tools/call` launch, injects resolved values only into the cleared child environment, and durable evidence carries only bounded secret reference fingerprints/counts. | Use as the baseline for MCP-S7 executable identity. |
| `mcp-executable-identity` | P1 | MCP server executable identity | implemented sufficient | closed | Runtime computes bounded executable content identity for the trusted absolute stdio executable before `tools/list`, includes executable identity fingerprints in catalog/config/task-pinned provenance, and rechecks the identity immediately before `tools/list` and `tools/call`. Missing, non-regular, unreadable, oversized, drifting, or mismatched executable targets fail closed before spawn without ledgering raw command strings, absolute paths, canonical paths, or executable content. | Use this closed executable-identity baseline for MCP Runtime Safety Policy closeout. |
| `mcp-p1-transport-task-reevaluation` | P2 | P1 transport and task surfaces | runtime outside this P0 campaign | post-v0 | Streamable HTTP, remote MCP, OAuth, Apps, Roots, Elicitation, Sampling, long-running MCP tasks, and public registry work are intentionally excluded from this P0 safety closure. | Reevaluate only after stdio MCP safety closure is complete. |

Runtime Release Ready remains `false` while the previously recorded Runtime
Release Readiness blockers remain open.
