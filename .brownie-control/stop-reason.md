# Brownie Stop Reason Pointer

This repository file is not the live stop reason.

Scheduled Brownie phase-loop stop reasons are recorded in the external automation root:

```text
~/.codex/automations/brownie-phase-loop/stop-reason.md
```

Use the external authoritative state before deciding whether the controller is blocked, waiting, ready to implement, or ready to review.
